# CV LaTeX replica

This folder contains the editable LaTeX reconstruction of `../cv.pdf`.

## Build

The document uses Times New Roman and should be compiled with XeLaTeX:

```sh
xelatex cv.tex
xelatex cv.tex
```

The output is `cv.pdf`. Keep `photo.png` beside `cv.tex` when compiling.
